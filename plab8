arr = [1,2,3,4,5,6]
target = 3
mid = len(arr)//2
high = len(arr)-1
low = 0
while low <= high:
    if arr[mid] == target:
        print("Element found at index", mid)
        break
    elif arr[mid] < target:
        low = mid + 1
    else:
        high = mid - 1
    mid = (low + high) // 2